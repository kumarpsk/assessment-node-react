Please find below the task for coding assignment.  
Create a contact form in React.  
Contact form should have following fields – First Name, Last Name, Email, Country, Phone, About. First Name and Last Name and Email should be mandatory for creating a new contact.  
Show a listing page of all the contacts in a card view.  
User can add a new contact or edit/update an existing contact.  
Create an API in NodeJS for CRUD operation on contact.  
Store the contact data in Mongo database.  
The contact form should have proper validation and the API should also have proper validation.  
The form component must be reusable.  
  
Note:   
Use React version 17+  
Use only functional components  
Use NodeJS version 16+  
Use Hapi and Express for backend API  
